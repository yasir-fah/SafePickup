# hooks/

This folder contains custom React hooks that encapsulate reusable logic.

## Structure
```
hooks/
├── useAuth.ts         # authentication state & actions
├── useCongestion.ts   # fetch & handle traffic congestion data
└── useLocation.ts     # get device GPS location
```